import React from "react";

const NoRecipes: React.FC = () => {
    return (
        <div>You have no saved recipes. Start by viewing recipes on the home screen and save the ones you like
            best.</div>
    )
}

export default NoRecipes